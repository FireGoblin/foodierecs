<h1>Listing mappings</h1>

<table>
  <tr>
    <th>Restaurant id1</th>
    <th>Restaurant id2</th>
    <th>Foodies</th>
    <th>Nonfoodie</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @mappings.each do |mapping| %>
  <tr>
    <td><%= mapping.restaurant_id1 %></td>
    <td><%= mapping.restaurant_id2 %></td>
    <td><%= mapping.foodies %></td>
    <td><%= mapping.nonfoodie %></td>
    <td><%= link_to 'Show', mapping %></td>
    <td><%= link_to 'Edit', edit_mapping_path(mapping) %></td>
    <td><%= link_to 'Destroy', mapping, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Mapping', new_mapping_path %>
