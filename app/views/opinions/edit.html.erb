<%= form_for :Opinion, @opinion, :url => { :action => "edit" }, :html => {:class => "nifty_form"} do |f| %>
  <%= f.text_field :to_eat %>
  <%= f.text_field :to_drink %>
  <%= submit_tag "Submit" %>
<% end %>
